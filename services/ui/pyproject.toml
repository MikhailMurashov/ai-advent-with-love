[project]
name = "llm-ui"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "chainlit>=1.0",
    "httpx[sse]>=0.27",
    "websockets>=12.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
